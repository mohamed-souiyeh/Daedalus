import { Grid } from "../grid";
import { algoState } from "../types/algos.types";


export function randomWalkDFS(grid: Grid): algoState {
  console.log("bibop randome walk DFS algo is running");
  return algoState.done;
}
